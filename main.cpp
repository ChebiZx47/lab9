#include "std.h"
//  Эта программа является учебной
int main() {
    int a, b;
    cout << "Enter two integers: ";
    cin >> a >> b;

    cout << "Sum: " << (a + b) << endl;
    cout << "Del: " << (a/b) << endl;
    cout << "Proiz: " << (a*b) << endl;

    return 0;
}
